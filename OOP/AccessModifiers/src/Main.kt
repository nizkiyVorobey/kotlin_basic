fun main() {
    /**
     * 1. private
     * Працює так само як і в JAVA, поля, методи й класи, доступні тільки в середині класу
     *
     * 2. public
     * Працює так само як і в JAVA, всі поля, методи й класи, доступні в будь-якому місці програми. У Kotlin він стоїть за замовченням
     *
     * 3. protected
     * Працює так само як і в JAVA, доступний тільки всередині класу і з середини класів нащадків
     *
     * 4. internal
     * Видимість тільки в межах одного модуля
     */
}